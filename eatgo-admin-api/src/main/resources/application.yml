spring:
  datasource:
    url: jdbc:h2:~/data/eatgo
    platform: h2
    username:
    password:
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update

---

spring:
  profiles: test
  datasource:
    url: jdbc:h2:mem:test